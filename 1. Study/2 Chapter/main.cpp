#include <bits/stdc++.h>

// сокращение кода
#define REP(a, i, b) for (int i = a; i < b; i++)
using namespace std;
using ll = long long;
typedef long double ld;

int main() {
    
    // переназначение потоков ввода-вывода в файлы
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);

    // ускоряют ввод-вывод
    ios::sync_with_stdio(0);
    cin.tie(0);
    
    // чтение строки
    string s;
    getline(cin, s);

    // если кол-во данных неизвестно
    while(cin >> s) {

    }

    std::cout << -5 % 3;
    return 0;
}